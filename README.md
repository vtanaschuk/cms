
Before start:
1. Clone the project
2. ```composer install```
3. Create file ```app/config/db.php```, example:

```
<?php

$dbConfig = [
    'database_type' => 'mysql',
    'database_name' => 'db_name',
    'server' => 'localhost',
    'username' => 'user',
    'password' => 'password'
];
```
